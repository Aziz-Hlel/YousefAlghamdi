import { categoriesType } from "../categories.subcategories.types";

const estateTypes = [
    "apartement",
    "house",
    "Office",
    "land",
]

export default estateTypes;


categoriesType["Commercial Properties"]