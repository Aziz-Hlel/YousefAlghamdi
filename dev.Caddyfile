:80 {
  reverse_proxy /api/* server:5000
  reverse_proxy /* client:3000
}

:443 {
  reverse_proxy /api/* server:5000
  reverse_proxy /* client:3000

  # Use Caddy's internal certificate authority (CA) for local SSL during development
  # This avoids issues with certificate validation when no public domain is used.
  tls internal

}